export default function DashboardPage() {
    return (
        <div style={{
            minHeight: "100vh",
            display: "flex",
            alignItems: "center",
            justifyContent: "center",
            background: "#07090f",
            color: "#f0f4ff",
            fontFamily: "Inter, sans-serif",
            fontSize: "18px",
            gap: "12px"
        }}>
            <span>🚛</span>
            <span>Dashboard — coming next!</span>
        </div>
    );
}
